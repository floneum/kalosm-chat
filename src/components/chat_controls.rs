use dioxus::prelude::*;
use serde::{Deserialize, Serialize};
use crate::components::*;

#[component]
pub fn ChatControls() -> Element {
    rsx! {
            div {
                    class: "flex items-center px-4 py-3 space-x-3",

                    // Persona selector - compact
                    

                    // Model selector - takes most space
                   

                    // Model selection menu
                   
            

                    // Persona selection menu
                    
           }
    }
}